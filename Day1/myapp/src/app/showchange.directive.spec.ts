import { ShowchangeDirective } from './showchange.directive';

describe('ShowchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowchangeDirective();
    expect(directive).toBeTruthy();
  });
});
